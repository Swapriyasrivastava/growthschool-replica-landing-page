const Header = () => {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-black/80 backdrop-blur-sm border-b border-gray-800">
      <div className="container mx-auto px-4 py-4">
        {/* Empty header - all content removed */}
      </div>
    </header>
  );
};

export default Header;
